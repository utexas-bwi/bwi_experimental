%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% initial state (for testing)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%-served(P,O,0) :- thing(P), item(O).
%-waiting(O,0) :- item(O). 
%-loaded(O,0) :- item(O). 
%-closeto(P,0) :- thing(P). 

%at(f1_atrium,0). 
 at(f3_410,0).
%at(o3_426,0).
-facing(D,0) :- door(D).
-beside(D,0) :- door(D).
