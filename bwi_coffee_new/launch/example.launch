<launch>

  <!-- launch the planner -->
  <include file="$(find bwi_coffee_new)/launch/planner.launch">
    <arg name="dry_run" value="true" />
  </include>

  <!-- launch the gui -->
  <node name="segbot_gui" pkg="segbot_gui" type="question_dialog_plugin" />

</launch>
